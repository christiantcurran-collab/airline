# FlightLedger Frontend

Local React dashboard that visualizes canonical events from the backend API.

## Run

```powershell
cd C:\Users\ccurr\Desktop\Airline\frontend
npm.cmd install
npm.cmd run dev
```

The app expects backend API on `http://127.0.0.1:8000` and proxies `/api/*`.

